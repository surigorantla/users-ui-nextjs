## users-ui-next-js


## Setup
Follow the below steps to run project

1. Clone repository
2. Run `npm install` command to install dependencies
3. Execute `npm run dev` command to run the project in
4. Execute `npm run test` command to test the project 



